<% if signed_in? %>
<h2> You are alread signed in as <%= current_user.email %>. </h2>
<br>
To switch accounts <br> 
<%= link_to 'Sign out', signout_path, :class=>"btn btn-primary btn-lg" %>

<% else %>
<h1>New user</h1>

<%= render 'form' %>

<%= link_to 'Back', users_path %>

<% end %>